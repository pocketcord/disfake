name: Run Tests
on: [push, pull_request]


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: teaishealty/disfake/.github/setup.yaml
      - name: Run Tests
        run: poetry run pytest
